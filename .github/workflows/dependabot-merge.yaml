# Copyright 2026 BitWise Media Group Ltd
# SPDX-License-Identifier: MIT

# Caller for the reusable Dependabot auto-merge workflow. When a Dependabot PR's checks
# finish green, the reusable workflow approves it if it is a minor or patch update and
# merges it via the ff-merge action (major updates wait for a human). This keeps the
# reusable-workflow SHA pins, npm deps, and the toolchain submodule (.mise/) fresh with no human in
# the loop.
#
# Just workflow_run: no pull_request_target and no skipped-job clutter on the PR. The
# workflow_run list names every check that must be green -- "Continuous Integration" and
# "Security Analysis"; whichever finishes last triggers the merge attempt, and ff-merge
# re-verifies all checks. check_suite is NOT usable -- GitHub does not fire it for a
# repo's own Actions CI.
#
# Requires .github/dependabot.yaml (present) and the same org prerequisites as merge.yaml
# (the FF_MERGE_CLIENT_ID variable + FF_MERGE_PRIVATE_KEY secret, and the "FF Merge" App
# in main's ruleset bypass list). Requires branch protection that requires PR review.
#
# Pinned to a full commit SHA; Dependabot keeps the pin fresh.
name: Dependabot Auto-Merge

on:
  workflow_run:
    workflows: ["Continuous Integration", "Security Analysis"]
    types: [completed]

# the App token does the privileged work; the caller grants nothing
permissions: {}

jobs:
  auto-merge:
    uses: bitwise-media-group/github-workflows/.github/workflows/dependabot-merge.yaml@b9c031429f149794bedb5411b08d5a787b8d9959 # v6.0.1
    with:
      app-client-id: ${{ vars.FF_MERGE_CLIENT_ID }}
    secrets:
      app-private-key: ${{ secrets.FF_MERGE_PRIVATE_KEY }}
